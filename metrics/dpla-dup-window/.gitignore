# Ignore everything (the bot password file is materialized at runtime from
# the PYWIKIBOT_PASSWORD secret and must never be committed)...
*
# ...except the site config (not sensitive), this ignore file, and the
# release job.
!.gitignore
!user-config.py
!release_window.py
